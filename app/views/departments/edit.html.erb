<h2 class='listing-title'>Edit current department of <%= @department.name %></h2>
<%= render "form" %>

<div class='delete-form'>
	<%= link_to "Delete Department of #{@department.name}", department_path(@department), method: :delete, data: {confirm: "Are you sure you want to delete this department?"}, :class => 'submit-button'  %>
</div>


