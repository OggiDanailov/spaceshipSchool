<h2 class='listing-title'>Edit <%=@article.title %></h2>

<%= render "form" %>

<% if @article.instructor_id == current_instructor.id %>
	<%= link_to "Delete Article", article_path(@article), method: :delete, data: {confirm: "Are you sure you want to delete this article?"}, :class => 'submit-button' %>
<% end %>