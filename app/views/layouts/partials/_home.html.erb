    <% if !student_signed_in? && !admin_signed_in? && !instructor_signed_in? %>

<div class="wrapper">
    <ul class="navbar">
      <li class="active"><%= link_to "Home", root_path %></li>
      <li><%= link_to "About", about_path %></li>
      <li><%= link_to "Contact", about_path %></li>
      <li class="dropdown">
        <a href="#" class="dropbtn">Students</a>
        <div class="dropdown-content">
          <li class='invisible'><%= link_to "Sign up", new_student_registration_path  %></li>
          <li class='invisible'><%= link_to "Sign in", new_student_session_path  %></li>
        </div>
      </li>
    </ul>

   <% end %>


     

